__pycache__/
*.py[cod]
.DS_Store

# Local match records, when redirected into the project folder.
records.json
records.txt
records.json.tmp

# Working notes between sessions: local only, never pushed.
handoff.md
